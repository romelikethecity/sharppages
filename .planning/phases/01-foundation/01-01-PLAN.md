---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - assets/logos/logo-full-dark.svg
  - assets/logos/logo-full-light.svg
  - assets/logos/icon-dark.svg
  - assets/logos/icon-light.svg
  - assets/logos/wordmark-dark.svg
  - assets/logos/wordmark-light.svg
  - assets/favicons/favicon.svg
  - assets/favicons/favicon-32.png
  - assets/favicons/favicon-16.png
  - assets/favicons/apple-touch-icon.png
  - assets/favicons/icon-192.png
  - assets/favicons/icon-512.png
  - assets/og/og-default.png
  - assets/og/og-default.svg
  - tokens.css
  - site.webmanifest
  - scripts/nav_config.py
  - scripts/templates.py
  - css/styles.css
  - js/main.js
autonomous: true
requirements:
  - INFR-02
  - INFR-03
  - INFR-04
  - INFR-05
  - INFR-09
  - INFR-10
  - SEO-01
  - SEO-02
  - SEO-03
  - SEO-04
  - SEO-05
  - SEO-07
  - QUAL-04
  - PERF-01
  - PERF-02
  - PERF-03
  - PERF-05

must_haves:
  truths:
    - "Brand assets (logos, favicons, OG images, tokens.css, webmanifest) exist at correct paths"
    - "nav_config.py exports SITE_NAME, SITE_URL, SITE_TAGLINE, COPYRIGHT_YEAR, CTA_HREF, CTA_LABEL, NAV_ITEMS, FOOTER_COLUMNS"
    - "templates.py provides get_html_head, get_nav_html, get_footer_html, get_breadcrumb_schema, get_breadcrumb_html, generate_faq_html, generate_cta_section, get_page_wrapper, write_page"
    - "Generated HTML includes unique meta title, description, canonical URL, OG tags, Twitter Card, GA4 placeholder, Google Fonts with preconnect and display=swap"
    - "styles.css references tokens.css custom properties with BEM naming"
    - "main.js provides smooth scroll, header shadow on scroll, and form validation"
  artifacts:
    - path: "scripts/nav_config.py"
      provides: "Site config and nav data"
      exports: ["SITE_NAME", "SITE_URL", "SITE_TAGLINE", "COPYRIGHT_YEAR", "CTA_HREF", "CTA_LABEL", "NAV_ITEMS", "FOOTER_COLUMNS"]
    - path: "scripts/templates.py"
      provides: "Shared HTML generators"
      exports: ["get_html_head", "get_nav_html", "get_footer_html", "get_breadcrumb_schema", "get_breadcrumb_html", "generate_faq_html", "generate_cta_section", "get_page_wrapper", "write_page"]
    - path: "css/styles.css"
      provides: "Full site styling with dark theme"
      min_lines: 200
    - path: "js/main.js"
      provides: "Client-side interactivity"
      min_lines: 30
    - path: "tokens.css"
      provides: "CSS custom properties"
      contains: "--color-midnight"
  key_links:
    - from: "scripts/templates.py"
      to: "scripts/nav_config.py"
      via: "import"
      pattern: "from nav_config import"
    - from: "scripts/templates.py"
      to: "css/styles.css"
      via: "CSS link in get_html_head"
      pattern: "styles\\.css\\?v="
    - from: "scripts/templates.py"
      to: "js/main.js"
      via: "script tag in get_footer_html"
      pattern: "main\\.js"
---

<objective>
Create the shared foundation that every SharpPages page depends on: brand assets copied from the brand kit, nav_config.py as the single source of truth for site metadata and navigation, templates.py with all shared HTML generators adapted from Provyx for dark theme and Google Fonts, styles.css with BEM naming referencing tokens.css custom properties, and main.js for client-side interactivity.

Purpose: Plan 02 (build system) imports from nav_config.py and templates.py. Every page generated by build.py inherits its SEO meta tags, schema markup, navigation, footer, GA4 tracking, and brand styling from these foundation files.

Output: 6 source files (nav_config.py, templates.py, styles.css, main.js, tokens.css, site.webmanifest) + brand assets (logos, favicons, OG images) ready for build.py to consume.
</objective>

<execution_context>
@/Users/rome/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rome/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation/01-RESEARCH.md

<interfaces>
<!-- Reference patterns from Provyx that templates.py must adapt -->

From Provyx nav_config.py (adapt for SharpPages):
```python
SITE_NAME = "Provyx"
SITE_URL = "https://getprovyx.com"
SITE_TAGLINE = "Healthcare Provider Intelligence"
COPYRIGHT_YEAR = "2026"
CTA_HREF = "/contact/"
CTA_LABEL = "Get Provider Data"
NAV_ITEMS = [...]  # List of dicts with href, label, optional children
FOOTER_COLUMNS = {...}  # Dict of heading -> list of link dicts
```

From Provyx templates.py (function signatures to replicate):
```python
BASE_URL = "https://getprovyx.com"
CSS_VERSION = "11"
OUTPUT_DIR = "output"  # NEW: Provyx writes in-place, SharpPages uses output/

def get_html_head(title, description, canonical_path, extra_schema="", noindex=False, og_type="website") -> str
def get_nav_html(active_page=None) -> str
def get_footer_html() -> str
def get_breadcrumb_schema(breadcrumbs) -> str
def get_breadcrumb_html(breadcrumbs) -> str
def generate_faq_html(faqs, heading="Frequently Asked Questions") -> str
def generate_cta_section(title, text, button_text=None, button_href=None, include_form=False, formspree_id="") -> str
def get_page_wrapper(title, description, canonical_path, body_content, active_page=None, extra_schema="", noindex=False, og_type="website") -> str
def write_page(path, html) -> None
```

From brand kit tokens.css (key variables for styles.css):
```css
--color-bg: #06090E;        /* body background */
--color-surface: #0C1118;   /* footer, elevated surfaces */
--color-card: #111820;      /* card backgrounds */
--color-border: #1E2A38;    /* borders */
--color-text: #E4E8ED;      /* primary text */
--color-text-muted: #7A8899; /* secondary text */
--color-primary: #00C2E0;   /* cyan CTAs, links */
--color-accent: #E8B931;    /* gold highlights, sparingly */
--font-heading: 'Instrument Sans', system-ui, sans-serif;
--font-body: 'DM Sans', system-ui, sans-serif;
```

From brand kit head-snippet.html (exact font loading):
```html
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Instrument+Sans:wght@400;500;600;700&family=DM+Sans:ital,wght@0,400;0,500;0,700;1,400&display=swap" rel="stylesheet">
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Copy brand assets and create nav_config.py</name>
  <files>
    assets/logos/logo-full-dark.svg, assets/logos/logo-full-light.svg, assets/logos/icon-dark.svg, assets/logos/icon-light.svg, assets/logos/wordmark-dark.svg, assets/logos/wordmark-light.svg,
    assets/favicons/favicon.svg, assets/favicons/favicon-32.png, assets/favicons/favicon-16.png, assets/favicons/apple-touch-icon.png, assets/favicons/icon-192.png, assets/favicons/icon-512.png,
    assets/og/og-default.png, assets/og/og-default.svg,
    tokens.css, site.webmanifest,
    scripts/nav_config.py
  </files>
  <action>
    1. Create project directories: `mkdir -p assets/logos assets/favicons assets/og css js scripts`

    2. Copy brand assets from `/Users/rome/Downloads/sharppages-brand/`:
       - `cp logos/* assets/logos/` (6 SVG files)
       - `cp favicons/* assets/favicons/` (6 files: favicon.svg, favicon-32.png, favicon-16.png, apple-touch-icon.png, icon-192.png, icon-512.png)
       - `cp og/* assets/og/` (og-default.png, og-default.svg)
       - `cp tokens.css tokens.css` (project root, for @import from styles.css)
       - `cp site.webmanifest site.webmanifest` (project root)

    3. Create `scripts/nav_config.py` with these exact exports:
       ```python
       SITE_NAME = "SharpPages"
       SITE_URL = "https://sharppages.com"
       SITE_TAGLINE = "Sharp pages. Full rooms."
       COPYRIGHT_YEAR = "2026"
       CTA_HREF = "/contact/"
       CTA_LABEL = "Book a Call"
       ```

       NAV_ITEMS (flat, no dropdowns -- SharpPages is simpler than Provyx):
       ```python
       NAV_ITEMS = [
           {"href": "/services/", "label": "Services"},
           {"href": "/pricing/", "label": "Pricing"},
           {"href": "/work/", "label": "Work"},
           {"href": "/about/", "label": "About"},
           {"href": "/blog/", "label": "Blog"},
       ]
       ```

       FOOTER_COLUMNS (4 columns):
       ```python
       FOOTER_COLUMNS = {
           "Services": [
               {"href": "/services/", "label": "All Services"},
               {"href": "/pricing/", "label": "Pricing"},
               {"href": "/work/", "label": "Our Work"},
               {"href": "/contact/", "label": "Contact"},
           ],
           "Industries": [
               {"href": "/for/medical-device-companies/", "label": "Medical Devices"},
               {"href": "/for/pharma-field-marketing/", "label": "Pharma"},
               {"href": "/for/b2b-saas/", "label": "B2B SaaS"},
               {"href": "/for/conference-organizers/", "label": "Conferences"},
               {"href": "/for/med-spas/", "label": "Med Spas"},
               {"href": "/for/real-estate/", "label": "Real Estate"},
           ],
           "Resources": [
               {"href": "/blog/", "label": "Blog"},
               {"href": "/about/", "label": "About"},
           ],
           "Legal": [
               {"href": "/privacy/", "label": "Privacy Policy"},
               {"href": "/terms/", "label": "Terms of Service"},
           ],
       }
       ```

    4. Add `scripts/__init__.py` (empty file) so `from scripts.nav_config import ...` works.
  </action>
  <verify>
    <automated>cd /Users/rome/Documents/projects/sharppages && test -f assets/logos/logo-full-dark.svg && test -f assets/favicons/favicon.svg && test -f assets/og/og-default.png && test -f tokens.css && test -f site.webmanifest && python3 -c "from scripts.nav_config import SITE_NAME, SITE_URL, NAV_ITEMS, FOOTER_COLUMNS; assert SITE_NAME == 'SharpPages'; assert len(NAV_ITEMS) == 5; print('OK')"</automated>
  </verify>
  <done>All brand assets exist at correct paths. nav_config.py imports successfully and exports all required constants with SharpPages values.</done>
</task>

<task type="auto">
  <name>Task 2: Create templates.py, styles.css, and main.js</name>
  <files>scripts/templates.py, css/styles.css, js/main.js</files>
  <action>
    **scripts/templates.py** -- Adapt from Provyx templates.py with these key differences:

    Constants:
    ```python
    BASE_URL = "https://sharppages.com"
    CSS_VERSION = "1"
    OUTPUT_DIR = "output"
    ```

    `get_html_head(title, description, canonical_path, extra_schema="", noindex=False, og_type="website")`:
    - Returns `<!DOCTYPE html><html lang="en"><head>...</head>`
    - `<meta name="theme-color" content="#06090E">` (Midnight, not Provyx's #1B2A4A)
    - Google Fonts via CDN with preconnect (from head-snippet.html), NOT self-hosted:
      ```
      <link rel="preconnect" href="https://fonts.googleapis.com">
      <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
      <link href="https://fonts.googleapis.com/css2?family=Instrument+Sans:wght@400;500;600;700&family=DM+Sans:ital,wght@0,400;0,500;0,700;1,400&display=swap" rel="stylesheet">
      ```
    - Favicons from `/assets/favicons/` (matching brand kit paths in head-snippet.html):
      ```
      <link rel="icon" href="/assets/favicons/favicon.svg" type="image/svg+xml">
      <link rel="icon" href="/assets/favicons/favicon-32.png" sizes="32x32" type="image/png">
      <link rel="icon" href="/assets/favicons/favicon-16.png" sizes="16x16" type="image/png">
      <link rel="apple-touch-icon" href="/assets/favicons/apple-touch-icon.png">
      ```
    - PWA manifest: `<link rel="manifest" href="/site.webmanifest">`
    - CSS link: `<link rel="stylesheet" href="/css/styles.css?v={CSS_VERSION}">` (NOT min, no tokens.css link -- tokens are @imported in styles.css)
    - GA4 with Consent Mode v2 using `G-PLACEHOLDER`:
      ```javascript
      window.dataLayer = window.dataLayer || [];
      function gtag(){dataLayer.push(arguments);}
      gtag('consent', 'default', {
        'ad_storage': 'denied', 'analytics_storage': 'denied',
        'ad_user_data': 'denied', 'ad_personalization': 'denied'
      });
      var _sc = localStorage.getItem('sharppages-consent');
      if (_sc === 'granted') {
        gtag('consent', 'update', {
          'ad_storage': 'granted', 'analytics_storage': 'granted',
          'ad_user_data': 'granted', 'ad_personalization': 'granted'
        });
      }
      ```
      Then async gtag script + config with G-PLACEHOLDER.
    - OG tags: og:type, og:url, og:title, og:description, og:site_name, og:image (default to `/assets/og/og-default.png`)
    - Twitter Card: summary_large_image with title, description, image
    - Canonical URL with trailing slash enforcement
    - `<meta name="robots" content="max-snippet:-1, max-image-preview:large, max-video-preview:-1">` (or noindex if flag set)
    - CRITICAL: All JS braces in f-strings must be doubled `{{` `}}`

    `get_nav_html(active_page=None)`:
    - Returns `<body>` + skip-to-content link + `<header>` + nav + mobile nav + inline JS toggle + `<main id="main-content">`
    - Logo: `<img src="/assets/logos/logo-full-dark.svg" alt="SharpPages" class="header__logo-icon" width="140" height="32" fetchpriority="high">` (use full logo SVG, not just icon)
    - No dropdowns needed (NAV_ITEMS are all flat)
    - Mobile nav with hamburger toggle (same inline JS pattern as Provyx, doubled braces)
    - CTA button in header: `<a href="{CTA_HREF}" class="btn btn--primary btn--sm">{CTA_LABEL}</a>`

    `get_footer_html()`:
    - Returns `</main>` + `<footer>` + consent banner + `<script src="/js/main.js" defer>` + `</body></html>`
    - Footer logo: use `/assets/logos/logo-full-dark.svg`
    - Footer columns from FOOTER_COLUMNS
    - Copyright: `&copy; {COPYRIGHT_YEAR} {SITE_NAME}. All rights reserved.`
    - Consent banner with sharppages-consent localStorage key (not provyx-consent)
    - Consent banner function name: `sharpConsent` (not provyxConsent)

    `get_breadcrumb_schema(breadcrumbs)` and `get_breadcrumb_html(breadcrumbs)`:
    - Identical pattern to Provyx. JSON-LD BreadcrumbList schema. Visible nav with separator.

    `generate_faq_html(faqs, heading="Frequently Asked Questions")`:
    - Uses `<details>/<summary>` (native accordion, zero JS). Includes FAQPage JSON-LD schema.

    `generate_cta_section(title, text, button_text=None, button_href=None, include_form=False, formspree_id="")`:
    - CTA section with surface background. Button or Formspree form.

    `get_page_wrapper(title, description, canonical_path, body_content, active_page=None, extra_schema="", noindex=False, og_type="website")`:
    - Combines get_html_head + get_nav_html + body_content + get_footer_html.

    `write_page(path, html)`:
    - Writes to `OUTPUT_DIR/path`. Uses `os.path.dirname(os.path.dirname(os.path.abspath(__file__)))` for project root, then joins with OUTPUT_DIR.
    - Creates directories as needed. Prints `Generated: /path`.

    ---

    **css/styles.css**:
    - First line: `@import url('/tokens.css');`
    - BEM naming throughout
    - Key sections (approx 300-400 lines):
      - `.container` -- max-width: var(--max-width), centered, padding
      - `.header` -- fixed, rgba(6,9,14,0.95) bg, backdrop-blur, z-index 1000, border-bottom
      - `.header__inner` -- flex, space-between, align-center
      - `.header__logo` -- flex, align-center, no text-decoration
      - `.header__logo-icon` -- height 32px auto width
      - `.nav--desktop` -- hidden on mobile, flex on 768px+
      - `.nav__list` -- flex, list-style none, gap
      - `.nav__link` -- color text-muted, hover primary, font-weight 500
      - `.nav__link--active` -- color primary
      - `.menu-toggle` -- hamburger icon, visible on mobile, hidden 768px+
      - `.nav--mobile` -- fixed overlay, transform translateX(100%), transition, bg surface
      - `.nav--mobile.active` -- translateX(0)
      - `.btn` -- inline-flex, padding, border-radius var(--radius-md), font-weight 600, transition
      - `.btn--primary` -- bg primary, color text-inverse, hover primary-hover
      - `.btn--sm` -- smaller padding
      - `.section` -- padding var(--space-20) 0
      - `.footer` -- bg surface, padding var(--space-16) 0, border-top
      - `.footer__grid` -- grid, 5 columns on desktop (1 brand + 4 link columns), 2 on tablet, 1 on mobile
      - `.footer__heading` -- text-sm, uppercase, tracking-wide, text-muted, margin-bottom
      - `.footer__links` -- list-style none, links text-muted hover primary
      - `.footer__bottom` -- border-top, padding-top, text-sm, text-muted
      - `.breadcrumb` -- text-sm, text-muted, margin-bottom
      - `.faq-item` -- border-bottom, padding
      - `.faq-item__question` -- cursor pointer, font-weight 600, padding
      - `.faq-item__answer` -- padding, text-muted
      - `.consent-banner` -- fixed bottom, bg card, padding, border-top, z-index 999
      - `.sr-only` -- visually hidden, `.sr-only--focusable:focus` visible
      - Responsive breakpoints: 768px (tablet), 1024px (desktop)
    - CRITICAL: Never hardcode colors. Always use var(--color-*) from tokens.css.
    - CRITICAL: Ensure contrast. Primary text (#E4E8ED) on bg (#06090E) = 13.9:1. Muted text (#7A8899) on bg = 5.2:1. Do NOT use --color-text-subtle for readable text (fails contrast).

    ---

    **js/main.js**:
    - Header shadow on scroll (add `.header--scrolled` class when scrollY > 10, add `box-shadow` via CSS)
    - Smooth scroll for anchor links
    - Form validation: prevent submit if required fields empty, validate email format, show inline errors. Honeypot field (`_gotcha`) support for Formspree.
    - GA4 event helper: `function trackEvent(action, category, label)` that calls gtag if available
    - Keep it under 60 lines. No libraries.
  </action>
  <verify>
    <automated>cd /Users/rome/Documents/projects/sharppages && python3 -c "from scripts.templates import get_page_wrapper, write_page, get_html_head, get_nav_html, get_footer_html, get_breadcrumb_schema, get_breadcrumb_html, generate_faq_html, generate_cta_section; print('All functions imported OK')" && test -f css/styles.css && test -f js/main.js && grep '@import' css/styles.css && grep 'var(--color' css/styles.css | head -3 && echo "PASS"</automated>
  </verify>
  <done>templates.py imports successfully with all 9 public functions. get_html_head produces HTML with Google Fonts preconnect + display=swap, GA4 placeholder (G-PLACEHOLDER), OG tags, Twitter Card, canonical URL, and BreadcrumbList schema support. styles.css uses @import tokens.css and references CSS custom properties with BEM naming. main.js provides scroll header, smooth scroll, form validation, and GA4 event tracking in under 60 lines.</done>
</task>

</tasks>

<verification>
After both tasks complete:

1. Import check: `python3 -c "from scripts.nav_config import *; from scripts.templates import *; print('Imports OK')"`
2. Brand assets: `ls assets/logos/ assets/favicons/ assets/og/` shows all expected files
3. HTML output test: `python3 -c "from scripts.templates import get_page_wrapper; html = get_page_wrapper('Test', 'Test description.', '/test/', '<p>Hello</p>'); assert 'G-PLACEHOLDER' in html; assert 'preconnect' in html; assert 'display=swap' in html; assert 'sharppages.com/test/' in html; assert 'og:title' in html; assert 'twitter:card' in html; print('HTML OK')"`
4. CSS tokens: `grep 'var(--color' css/styles.css | wc -l` returns 20+ (confirms token usage)
5. JS exists: `wc -l js/main.js` shows 30-60 lines
</verification>

<success_criteria>
- nav_config.py is importable and exports all 8 required constants/structures
- templates.py is importable and all 9 functions produce valid HTML strings
- Generated HTML head includes: Google Fonts with preconnect + display=swap, GA4 G-PLACEHOLDER, OG tags, Twitter Card, canonical URL, theme-color #06090E, favicon links
- styles.css @imports tokens.css and uses 20+ CSS custom property references
- js/main.js provides header scroll, smooth scroll, form validation in under 60 lines
- All brand assets present at assets/logos/, assets/favicons/, assets/og/
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
